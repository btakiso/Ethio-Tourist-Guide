import 'package:flutter/material.dart';

class DestinationGuidesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Destination Guides'),
      ),
      body: Center(
        child: Text('Welcome to Destination Guides'),
      ),
    );
  }
}